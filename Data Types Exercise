# Tes Menambah angka
# a = input("Ketik 2 digit angka: ")
# a1 = int(a[0])
# a2 = int(a[1])
# print(a1 + a2)

# Versi mbak angela
# two_digit_number = input("Type a two digit number: ")
# first_digit = two_digit_number[0]
# second_digit = two_digit_number[1]
# result = int(first_digit) + int(second_digit)
# print(result)
